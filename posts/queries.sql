select * from product;

-- view a list of employees //in sequel comments are dash dash
select * from employee;

-- selecting specific columns
select id, productName, supplierId 
from product;

select firstName, lastName, title from employee;